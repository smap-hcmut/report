apiVersion: v1
kind: Service
metadata:
  name: smap-analytics-api
  namespace: smap
  labels:
    app: smap-analytics-api
    component: api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: smap-analytics-api