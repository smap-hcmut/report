apiVersion: apps/v1
kind: Deployment
metadata:
  name: smap-youtube-scraper
  namespace: smap
  labels:
    app: smap-youtube-scraper
    component: worker
    version: v3
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: smap-youtube-scraper
  template:
    metadata:
      labels:
        app: smap-youtube-scraper
        component: worker
        version: v3
      annotations:
        prometheus.io/scrape: "false"
        config/hash: "PLACEHOLDER_CONFIG_HASH"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      imagePullSecrets:
        - name: harbor-secret
      containers:
        - name: smap-youtube-scraper
          image: registry.tantai.dev/smap/smap-youtube-scraper:251211-210149
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false # YouTube worker needs write access for temp files
            capabilities:
              drop:
                - ALL
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: WORKER_NAME
              value: "$(POD_NAME)"
          envFrom:
            - configMapRef:
                name: smap-youtube-scraper-config
            - secretRef:
                name: smap-youtube-scraper-secrets
          resources:
            requests:
              memory: "128Mi"
              cpu: "200m"
            limits:
              memory: "256Mi"
              cpu: "400m"
          livenessProbe:
            exec:
              command:
                - python
                - -c
                - "import sys; sys.exit(0)"
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            exec:
              command:
                - python
                - -c
                - "import sys; sys.exit(0)"
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: app-tmp
              mountPath: /app/tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 512Mi
        - name: app-tmp
          emptyDir:
            sizeLimit: 256Mi
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: smap-youtube-scraper
                topologyKey: kubernetes.io/hostname
      tolerations:
        - key: "node.kubernetes.io/memory-pressure"
          operator: "Exists"
          effect: "NoSchedule"
        - key: "node.kubernetes.io/disk-pressure"
          operator: "Exists"
          effect: "NoSchedule"
