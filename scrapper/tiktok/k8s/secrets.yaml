apiVersion: v1
kind: Secret
metadata:
  name: smap-tiktok-scraper-secrets
  namespace: smap
  labels:
    app: smap-tiktok-scraper
    component: secrets
    version: v3
type: Opaque
stringData:
  # ========== Database Credentials ==========
  MONGODB_USER: "smap" # Set in deployment
  MONGODB_PASSWORD: "hcmut2025" # Set in deployment

  # ========== MinIO Credentials ==========
  MINIO_ACCESS_KEY: "smap" # Set in deployment
  MINIO_SECRET_KEY: "hcmut2025" # Set in deployment

  # ========== RabbitMQ Credentials ==========
  RABBITMQ_USER: "smap"
  RABBITMQ_PASSWORD: "hcmut"
