// Định nghĩa các counter dùng để đánh số tự động bảng (table) và hình ảnh (image)
// table_counter: dùng cho bảng, image_counter: dùng cho hình ảnh

#let table_counter = counter("table")
#let image_counter = counter("image")
