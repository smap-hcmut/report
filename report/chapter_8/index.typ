// Chapter 8: Phụ lục

= CHƯƠNG 8: PHỤ LỤC

== Phụ lục A: Danh sách từ viết tắt
#text()[
  #set par(justify: false)
  #table(
    columns: (0.3fr, 1fr),
    stroke: 0.5pt,

    table.cell(align: center + horizon, inset: (y: 0.6em))[*Từ viết tắt*],
    table.cell(align: center + horizon, inset: (y: 0.6em))[*Nghĩa đầy đủ*],

    // A
    table.cell(align: center + horizon, inset: (y: 0.6em))[ABSA],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Aspect-Based Sentiment Analysis],
    table.cell(align: center + horizon, inset: (y: 0.6em))[AC],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Architecture Characteristic],
    table.cell(align: center + horizon, inset: (y: 0.6em))[ACID],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Atomicity, Consistency, Isolation, Durability],
    table.cell(align: center + horizon, inset: (y: 0.6em))[ADR],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Architecture Decision Record],
    table.cell(align: center + horizon, inset: (y: 0.6em))[AES],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Advanced Encryption Standard],
    table.cell(align: center + horizon, inset: (y: 0.6em))[AI],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Artificial Intelligence],
    table.cell(align: center + horizon, inset: (y: 0.6em))[AMQP],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Advanced Message Queuing Protocol],
    table.cell(align: center + horizon, inset: (y: 0.6em))[API],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Application Programming Interface],
    table.cell(align: center + horizon, inset: (y: 0.6em))[ASR],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Automatic Speech Recognition],

    // B
    table.cell(align: center + horizon, inset: (y: 0.6em))[BI],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Business Intelligence],
    table.cell(align: center + horizon, inset: (y: 0.6em))[BPMN],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Business Process Model and Notation],
    table.cell(align: center + horizon, inset: (y: 0.6em))[BR],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Business Rule],
    table.cell(align: center + horizon, inset: (y: 0.6em))[BSI],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Buzzmetrics Social Index],

    // C
    table.cell(align: center + horizon, inset: (y: 0.6em))[C4],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Context, Container, Component, Code (Model)],
    table.cell(align: center + horizon, inset: (y: 0.6em))[CDN],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Content Delivery Network],
    table.cell(align: center + horizon, inset: (y: 0.6em))[CI/CD],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Continuous Integration/Continuous Deployment],
    table.cell(align: center + horizon, inset: (y: 0.6em))[CORS],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Cross-Origin Resource Sharing],
    table.cell(align: center + horizon, inset: (y: 0.6em))[CPU],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Central Processing Unit],
    table.cell(align: center + horizon, inset: (y: 0.6em))[CRUD],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Create, Read, Update, Delete],

    // D
    table.cell(align: center + horizon, inset: (y: 0.6em))[DAO],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Data Access Object],
    table.cell(align: center + horizon, inset: (y: 0.6em))[DDD],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Domain-Driven Design],
    table.cell(align: center + horizon, inset: (y: 0.6em))[DNS],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Domain Name System],
    table.cell(align: center + horizon, inset: (y: 0.6em))[DRY],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Don't Repeat Yourself],
    table.cell(align: center + horizon, inset: (y: 0.6em))[DTO],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Data Transfer Object],

    // E
    table.cell(align: center + horizon, inset: (y: 0.6em))[ERD],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Entity Relationship Diagram],
    table.cell(align: center + horizon, inset: (y: 0.6em))[ETL],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Extract, Transform, Load],

    // F
    table.cell(align: center + horizon, inset: (y: 0.6em))[FFmpeg],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Fast Forward Moving Picture Experts Group],
    table.cell(align: center + horizon, inset: (y: 0.6em))[FR],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Functional Requirement],

    // G
    table.cell(align: center + horizon, inset: (y: 0.6em))[GDPR],
    table.cell(align: center + horizon, inset: (y: 0.6em))[General Data Protection Regulation],
    table.cell(align: center + horizon, inset: (y: 0.6em))[GIN],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Generalized Inverted Index],
    table.cell(align: center + horizon, inset: (y: 0.6em))[gRPC],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Google Remote Procedure Call],

    // H
    table.cell(align: center + horizon, inset: (y: 0.6em))[HTTP],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Hypertext Transfer Protocol],
    table.cell(align: center + horizon, inset: (y: 0.6em))[HTTPS],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Hypertext Transfer Protocol Secure],

    // I
    table.cell(align: center + horizon, inset: (y: 0.6em))[IAM],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Identity and Access Management],
    table.cell(align: center + horizon, inset: (y: 0.6em))[I/O],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Input/Output],
    table.cell(align: center + horizon, inset: (y: 0.6em))[IP],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Internet Protocol],

    // J
    table.cell(align: center + horizon, inset: (y: 0.6em))[JSON],
    table.cell(align: center + horizon, inset: (y: 0.6em))[JavaScript Object Notation],
    table.cell(align: center + horizon, inset: (y: 0.6em))[JSONB],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Binary JSON (PostgreSQL format)],
    table.cell(align: center + horizon, inset: (y: 0.6em))[JWT],
    table.cell(align: center + horizon, inset: (y: 0.6em))[JSON Web Token],

    // K
    table.cell(align: center + horizon, inset: (y: 0.6em))[K8s],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Kubernetes],
    table.cell(align: center + horizon, inset: (y: 0.6em))[KISS],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Keep It Simple, Stupid],
    table.cell(align: center + horizon, inset: (y: 0.6em))[KPI],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Key Performance Indicator],

    // M
    table.cell(align: center + horizon, inset: (y: 0.6em))[MinIO],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Minimal Object Storage],
    table.cell(align: center + horizon, inset: (y: 0.6em))[ML],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Machine Learning],
    table.cell(align: center + horizon, inset: (y: 0.6em))[MVC],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Model-View-Controller],
    table.cell(align: center + horizon, inset: (y: 0.6em))[MVCC],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Multi-Version Concurrency Control],

    // N
    table.cell(align: center + horizon, inset: (y: 0.6em))[NFR],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Non-Functional Requirement],
    table.cell(align: center + horizon, inset: (y: 0.6em))[NLP],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Natural Language Processing],
    table.cell(align: center + horizon, inset: (y: 0.6em))[NoSQL],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Not Only SQL],
    table.cell(align: center + horizon, inset: (y: 0.6em))[NSS],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Net Sentiment Score],

    // O
    table.cell(align: center + horizon, inset: (y: 0.6em))[OAuth],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Open Authorization],
    table.cell(align: center + horizon, inset: (y: 0.6em))[OLAP],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Online Analytical Processing],
    table.cell(align: center + horizon, inset: (y: 0.6em))[OOM],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Out of Memory],
    table.cell(align: center + horizon, inset: (y: 0.6em))[ORM],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Object-Relational Mapping],

    // P
    table.cell(align: center + horizon, inset: (y: 0.6em))[PDPA],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Personal Data Protection Act],
    table.cell(align: center + horizon, inset: (y: 0.6em))[PII],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Personally Identifiable Information],
    table.cell(align: center + horizon, inset: (y: 0.6em))[PM],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Project Manager],
    table.cell(align: center + horizon, inset: (y: 0.6em))[PostgreSQL],
    table.cell(align: center + horizon, inset: (y: 0.6em))[PostgreSQL Database],

    // R
    table.cell(align: center + horizon, inset: (y: 0.6em))[RabbitMQ],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Message Queue System (AMQP)],
    table.cell(align: center + horizon, inset: (y: 0.6em))[RAM],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Random Access Memory],
    table.cell(align: center + horizon, inset: (y: 0.6em))[RBAC],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Role-Based Access Control],
    table.cell(align: center + horizon, inset: (y: 0.6em))[RDBMS],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Relational Database Management System],
    table.cell(align: center + horizon, inset: (y: 0.6em))[RDB],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Redis Database (snapshot)],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Redis],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Remote Dictionary Server],
    table.cell(align: center + horizon, inset: (y: 0.6em))[REST],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Representational State Transfer],
    table.cell(align: center + horizon, inset: (y: 0.6em))[RFC],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Request for Comments],
    table.cell(align: center + horizon, inset: (y: 0.6em))[ROI],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Return on Investment],

    // S
    table.cell(align: center + horizon, inset: (y: 0.6em))[S3],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Simple Storage Service],
    table.cell(align: center + horizon, inset: (y: 0.6em))[SaaS],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Software as a Service],
    table.cell(align: center + horizon, inset: (y: 0.6em))[SDD],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Software Design Document],
    table.cell(align: center + horizon, inset: (y: 0.6em))[SMAP],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Social Media Analytics Platform],
    table.cell(align: center + horizon, inset: (y: 0.6em))[SOLID],
    table.cell(align: center + horizon, inset: (
      y: 0.6em,
    ))[Single Responsibility, Open-Closed, Liskov Substitution, Interface Segregation, Dependency Inversion],
    table.cell(align: center + horizon, inset: (y: 0.6em))[SoV],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Share of Voice],
    table.cell(align: center + horizon, inset: (y: 0.6em))[SQL],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Structured Query Language],
    table.cell(align: center + horizon, inset: (y: 0.6em))[SRS],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Software Requirements Specification],
    table.cell(align: center + horizon, inset: (y: 0.6em))[SSE],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Server-Sent Events],
    table.cell(align: center + horizon, inset: (y: 0.6em))[SSL],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Secure Sockets Layer],
    table.cell(align: center + horizon, inset: (y: 0.6em))[SSOT],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Single Source of Truth],
    table.cell(align: center + horizon, inset: (y: 0.6em))[SSR],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Server-Side Rendering],

    // T
    table.cell(align: center + horizon, inset: (y: 0.6em))[TCP],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Transmission Control Protocol],
    table.cell(align: center + horizon, inset: (y: 0.6em))[TDD],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Test-Driven Development],
    table.cell(align: center + horizon, inset: (y: 0.6em))[TLS],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Transport Layer Security],
    table.cell(align: center + horizon, inset: (y: 0.6em))[TTI],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Time-to-Insight],
    table.cell(align: center + horizon, inset: (y: 0.6em))[TTL],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Time to Live],

    // U
    table.cell(align: center + horizon, inset: (y: 0.6em))[UC],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Use Case],
    table.cell(align: center + horizon, inset: (y: 0.6em))[UI],
    table.cell(align: center + horizon, inset: (y: 0.6em))[User Interface],
    table.cell(align: center + horizon, inset: (y: 0.6em))[UML],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Unified Modeling Language],
    table.cell(align: center + horizon, inset: (y: 0.6em))[URI],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Uniform Resource Identifier],
    table.cell(align: center + horizon, inset: (y: 0.6em))[URL],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Uniform Resource Locator],
    table.cell(align: center + horizon, inset: (y: 0.6em))[UUID],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Universally Unique Identifier],
    table.cell(align: center + horizon, inset: (y: 0.6em))[UX],
    table.cell(align: center + horizon, inset: (y: 0.6em))[User Experience],

    // W
    table.cell(align: center + horizon, inset: (y: 0.6em))[WebSocket],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Web Socket Protocol],

    // Y
    table.cell(align: center + horizon, inset: (y: 0.6em))[YAGNI],
    table.cell(align: center + horizon, inset: (y: 0.6em))[You Aren't Gonna Need It],
  )
]


== Phụ lục B: Bảng thuật ngữ

#text()[
  #set par(justify: false)
  #table(
    columns: (0.3fr, 1fr),
    stroke: 0.5pt,
    align: (left + top, left + top),

    table.cell(align: center + horizon, inset: (y: 0.6em))[*Thuật ngữ*],
    table.cell(align: center + horizon, inset: (y: 0.6em))[*Định nghĩa*],

    // A
    table.cell(align: center + horizon, inset: (y: 0.6em))[Aspect Extraction],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Trích xuất các khía cạnh được đề cập trong nội dung văn bản, ví dụ như giá cả, chất lượng, dịch vụ.],

    // B
    table.cell(align: center + horizon, inset: (y: 0.6em))[Batch Processing],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Xử lý dữ liệu theo lô thay vì từng item riêng lẻ để tối ưu throughput và giảm overhead.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Bounded Context],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Phạm vi trong đó các thuật ngữ nghiệp vụ được hiểu nhất quán, là đơn vị cơ bản trong Domain-Driven Design.],

    // C
    table.cell(align: center + horizon, inset: (y: 0.6em))[Captcha],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Cơ chế xác thực người dùng thật của các platforms để chống bot tự động.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Circuit Breaker],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Pattern ngăn chặn cascade failures bằng cách tạm dừng gọi service khi phát hiện lỗi liên tục.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Claim Check Pattern],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Pattern tách payload lớn khỏi message, lưu vào object storage và chỉ gửi reference qua message queue.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Clean Architecture],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Kiến trúc phần mềm tách biệt business logic khỏi infrastructure thông qua các layers và dependency inversion.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Core Domain],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Miền nghiệp vụ trọng tâm tạo ra giá trị cốt lõi và lợi thế cạnh tranh của hệ thống.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Crawler Worker],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Thành phần thu thập dữ liệu từ các nền tảng mạng xã hội thông qua web scraping hoặc API.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Crisis Detection],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Phát hiện khủng hoảng truyền thông dựa trên sự tăng trưởng đột biến của thảo luận tiêu cực.],

    // D
    table.cell(align: center + horizon, inset: (y: 0.6em))[Dead Letter Queue],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Hàng đợi lưu trữ các messages không thể xử lý được để phân tích và retry sau.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Dependency Inversion],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Nguyên tắc thiết kế trong đó modules cấp cao không phụ thuộc vào modules cấp thấp, cả hai phụ thuộc vào abstractions.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Distributed State],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Quản lý trạng thái phân tán giữa nhiều services sử dụng cache tập trung như Redis.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Dry-run],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Chức năng kiểm tra chất lượng từ khóa bằng cách thu thập mẫu kết quả trước khi chạy project chính thức.],

    // E
    table.cell(align: center + horizon, inset: (y: 0.6em))[Event-Driven Architecture],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Kiến trúc trong đó các services giao tiếp thông qua events qua message broker thay vì gọi trực tiếp.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Eventual Consistency],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Mô hình nhất quán dữ liệu trong đó hệ thống đảm bảo dữ liệu sẽ nhất quán sau một khoảng thời gian.],

    // F
    table.cell(align: center + horizon, inset: (y: 0.6em))[Fanout Exchange],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Loại exchange trong RabbitMQ phát sóng messages đến tất cả queues được gắn kết.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Fault Isolation],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Khả năng cô lập lỗi để lỗi ở một service không ảnh hưởng đến các services khác.],

    // G
    table.cell(align: center + horizon, inset: (y: 0.6em))[Generic Domain],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Miền nghiệp vụ phổ biến có thể sử dụng giải pháp có sẵn hoặc dịch vụ bên thứ ba.],

    // H
    table.cell(align: center + horizon, inset: (y: 0.6em))[Health Check],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Endpoint kiểm tra trạng thái hoạt động của service, bao gồm liveness và readiness probes.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Hexagonal Architecture],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Kiến trúc phần mềm sử dụng ports và adapters để tách biệt business logic khỏi infrastructure.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[High Cohesion],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Nguyên tắc thiết kế trong đó các thành phần trong một module có mối liên hệ mạnh mẽ với nhau.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Horizontal Scaling],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Mở rộng hệ thống bằng cách thêm nhiều instances thay vì nâng cấp hardware của instance hiện tại.],

    // I
    table.cell(align: center + horizon, inset: (y: 0.6em))[Impact Score],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Điểm số đánh giá mức độ ảnh hưởng của nội dung dựa trên engagement và sentiment.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Intent Classification],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Phân loại ý định của nội dung văn bản thành các categories như review, question, complaint.],

    // J
    table.cell(align: center + horizon, inset: (y: 0.6em))[Job ID],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Mã định danh duy nhất cho mỗi tác vụ thu thập hoặc phân tích trong hệ thống.],

    // L
    table.cell(align: center + horizon, inset: (y: 0.6em))[Load Balancing],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Phân phối tải công việc đều giữa nhiều workers hoặc instances để tối ưu hiệu năng.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Loose Coupling],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Nguyên tắc thiết kế trong đó các services có sự phụ thuộc tối thiểu vào nhau.],

    // M
    table.cell(align: center + horizon, inset: (y: 0.6em))[Marketing Analyst],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Người sử dụng chính của hệ thống SMAP để theo dõi danh tiếng thương hiệu và phân tích đối thủ.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Master-Worker Pattern],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Pattern trong đó Master node phân phối tasks đến các Worker nodes để xử lý song song.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Message Acknowledgment],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Cơ chế xác nhận message đã được xử lý thành công trước khi xóa khỏi queue.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Message Queue],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Hàng đợi tin nhắn cho phép các services giao tiếp bất đồng bộ.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Microservices],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Kiến trúc phần mềm chia hệ thống thành các services nhỏ, độc lập, dễ mở rộng và bảo trì.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Monolithic Architecture],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Kiến trúc phần mềm trong đó toàn bộ ứng dụng được triển khai như một đơn vị duy nhất.],

    // N
    table.cell(align: center + horizon, inset: (y: 0.6em))[Net Sentiment Score],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Điểm cảm xúc ròng tính bằng công thức (Positive - Negative) / Total.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[NLP Pipeline],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Chuỗi xử lý ngôn ngữ tự nhiên bao gồm preprocessing, intent, keyword, sentiment và impact.],

    // O
    table.cell(align: center + horizon, inset: (y: 0.6em))[Object Storage],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Hệ thống lưu trữ dữ liệu phi cấu trúc dưới dạng objects với metadata, sử dụng MinIO.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Observability],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Khả năng theo dõi, giám sát và debug hệ thống thông qua logs, metrics và traces.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Orchestrator],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Thành phần điều phối và quản lý luồng xử lý giữa các components hoặc services.],

    // P
    table.cell(align: center + horizon, inset: (y: 0.6em))[Partial Result],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Kết quả không đầy đủ do lỗi, timeout hoặc hủy giữa chừng trong quá trình xử lý.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[PhoBERT],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Mô hình ngôn ngữ pre-trained cho tiếng Việt dựa trên kiến trúc BERT.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Pipeline Pattern],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Pattern xử lý dữ liệu qua chuỗi các bước tuần tự, mỗi bước là một module độc lập.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Playwright],
    table.cell(align: left + horizon, inset: (y: 0.6em))[Framework tự động hóa browser cho web scraping và testing.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Polyglot Persistence],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Chiến lược sử dụng nhiều loại database khác nhau cho các use cases khác nhau.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Port and Adapter],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Pattern tách biệt business logic khỏi infrastructure thông qua interfaces và implementations.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Project],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Dự án theo dõi thương hiệu bao gồm cấu hình từ khóa, đối thủ, nền tảng và thời gian.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Pub/Sub],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Mô hình publish-subscribe cho phép gửi thông báo real-time đến nhiều subscribers.],

    // R
    table.cell(align: center + horizon, inset: (y: 0.6em))[Rate Limit],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Giới hạn số lượng requests trong một khoảng thời gian để tránh quá tải hệ thống.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Repository Pattern],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Pattern trừu tượng hóa data access layer qua interfaces để tách biệt business logic khỏi storage.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Retention Window],
    table.cell(align: left + horizon, inset: (y: 0.6em))[Khoảng thời gian lưu trữ dữ liệu trước khi archive hoặc xóa.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Rolling Deployment],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Chiến lược triển khai cập nhật từng instance một để giảm thiểu downtime.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Routing Key],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Thuộc tính của message được exchange sử dụng để quyết định routing đến queue nào.],

    // S
    table.cell(align: center + horizon, inset: (y: 0.6em))[Sentiment Analysis],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Phân tích cảm xúc của nội dung văn bản thành tích cực, tiêu cực hoặc trung tính.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Share of Voice],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Tỷ lệ phần trăm thảo luận về thương hiệu so với tổng thảo luận trong ngành.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Skip Logic],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Logic bỏ qua các bước xử lý tốn kém cho nội dung spam hoặc không liên quan.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Social Listening],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Quá trình theo dõi và phân tích các cuộc thảo luận trên mạng xã hội về thương hiệu.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Social Media Platform],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Nền tảng mạng xã hội như YouTube, TikTok, Facebook cung cấp dữ liệu công khai.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Soft Delete],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Xóa logic bằng cách đánh dấu deleted_at thay vì xóa vật lý khỏi database.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Spike Detection],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Phát hiện sự tăng đột biến trong volume thảo luận để cảnh báo khủng hoảng.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Stateless Service],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Service không lưu trữ state nội bộ, cho phép scale và restart dễ dàng.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Strategy Pattern],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Pattern cho phép chọn algorithm tại runtime thông qua interface chung.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Supporting Domain],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Miền nghiệp vụ hỗ trợ cho Core Domain nhưng không phải yếu tố tạo nên sự khác biệt.],

    // T
    table.cell(align: center + horizon, inset: (y: 0.6em))[Throughput],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Số lượng items được xử lý trong một đơn vị thời gian, thường đo bằng items/phút.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Trend Detection],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Phát hiện xu hướng tự động dựa trên engagement và velocity của nội dung.],

    // W
    table.cell(align: center + horizon, inset: (y: 0.6em))[Web Scraping],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Kỹ thuật thu thập dữ liệu từ websites bằng cách phân tích HTML và tự động hóa browser.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Webhook],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Cơ chế callback HTTP cho phép service gửi thông báo đến service khác khi có sự kiện.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[WebSocket],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Giao thức cho phép giao tiếp hai chiều real-time giữa client và server.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Whisper],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Mô hình speech-to-text của OpenAI để chuyển đổi audio sang transcript.],
    table.cell(align: center + horizon, inset: (y: 0.6em))[Worker],
    table.cell(align: left + horizon, inset: (
      y: 0.6em,
    ))[Instance xử lý tasks từ queue, có thể scale độc lập theo workload.],
  )
]
